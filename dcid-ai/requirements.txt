fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
pydantic-settings>=2.2,<3.0
httpx>=0.27,<1.0
minio>=7.2,<8.0
pypdf>=4.2,<6.0
pytest>=8.0,<9.0

# --- Torch CPU-only — PHẢI cài trước sentence-transformers ---
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.3.1+cpu
torchvision==0.18.1+cpu

# --- Embedding pipeline (app/pipeline/embed.py) ---
sentence-transformers>=3.0,<4.0

# --- Vector index (app/pipeline/index.py) ---
chromadb>=1.5,<2.0

# --- LLM client — OpenAI-compatible, dùng cho LM Studio local server ---
openai>=1.40,<2.0

# --- Async task queue (Celery + Redis broker/backend) ---
celery[redis]>=5.3,<6.0
redis>=5.0,<6.0

# --- Celery monitoring UI (optional, truy cập tại http://localhost:5555) ---
flower>=2.0,<3.0
