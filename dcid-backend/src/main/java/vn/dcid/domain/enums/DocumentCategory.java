package vn.dcid.domain.enums;

/** Loại tài liệu kỹ thuật (khớp CHECK trong V2__documents.sql). */
public enum DocumentCategory {
    SOP,
    DRAWING,
    CIRCUIT,
    MAINTENANCE_LOG,
    SAFETY,
    OTHER
}
